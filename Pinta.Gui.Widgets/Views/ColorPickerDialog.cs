using Pinta.Core;

namespace Pinta.Gui.Widgets;

public sealed class ColorPickerDialog : Gtk.Dialog
{
	internal ColorPickerDialog (
		Gtk.Window? parentWindow,
		PaletteManager paletteManager,
		ColorPick adjustable,
		bool primarySelected,
		bool livePaletteMode,
		string windowTitle)
	{
		// --- TODO
	}

	// --- Any relevant members. This is the only class where public methods (including the constructor) should remain as before
}
